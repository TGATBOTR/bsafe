name: Hello World!

on:
  pull_request:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
    - uses: actions/checkout@v2.3.3
    - name: helloworld
      run: echo "Hello World"